<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- club 홍보  페이지에 보통 쓰일 쿼리들 모을 예정 -->
<mapper namespace="ClubAdvertise">

	<select id="readClubAdvertisePreview"
		resultType="com.walab.hgu.DTO.ClubAdvertiseDTO"
		parameterType="hashmap">
		SELECT title, content, originalUrl FROM hguclub.clubAdvertise
    	INNER JOIN hguclub.clubAdvertiseFile cAF on clubAdvertise.id = cAF.clubAdvertiseId WHERE cAF.order = 1;
	</select>

</mapper>