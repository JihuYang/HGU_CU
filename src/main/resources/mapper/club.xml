<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- club 소개 페이지에 보통 쓰일 쿼리들 모을 예정  -->
<mapper namespace="Club">

	<select id="readClubIntroductionPreview"
		resultType="com.walab.hgu.DTO.ClubDTO"
		parameterType="hashmap">
		SELECT clubName, u.name as clubCeoName, clubLocation, phone, category as categoryName, foundationDate,snsName, snsLink 
		FROM hguclub.club as CL
		INNER JOIN hguclub.category as CA ON CA.id = CL.categoryId
        INNER JOIN user u on CL.userId = u.id
        INNER JOIN clubSNS cS on CL.id = cS.clubId;
	</select>

</mapper>