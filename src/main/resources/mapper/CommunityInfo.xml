<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CommunityInfo">

	<select id="readCommunityInfo"
		resultType="com.walab.hgu.DTO.CommunityInfoDTO"
		parameterType="hashMap">
		SELECT c.id, name as writer, title, c.regdate, viewCount
		FROM hguclub.communityInfo as c
		INNER JOIN hguclub.user as u ON
		c.userId = u.id
		ORDER BY c.regdate DESC 
		LIMIT #{displayPost}, #{postNum}
	</select>

	<select id="readCommunityInfoDetail"
		resultType="com.walab.hgu.DTO.CommunityInfoDTO"
		parameterType="hashmap">
		SELECT cI.id, name as writer, title, content,
		cI.regdate, viewCount, originalUrl, realUrl, cIF.order
		FROM
		hguclub.communityInfo as cI ,hguclub.user u, hguclub.communityInfoFile
		as cIF
		WHERE cI.userId = u.id and cI.id = #{id} and cIF.communityInfoId = cI.id LIMIT 1
	</select>

	<select id="readRecentCommunityInfo" resultType="int"
		parameterType="hashmap">
		SELECT MAX(id) FROM hguclub.communityInfo
	</select>

	<!-- 커뮤니티 공지사항 글 작성 쿼리 -->
	<insert id="creatCommunityInfo"
		parameterType="com.walab.hgu.DTO.CommunityInfoDTO">
		INSERT INTO hguclub.communityInfo (userId, title,
		content)
		VALUES (#{userId}, #{title}, #{content})
	</insert>

	<!-- 커뮤니티 공지사항 파일 업로드 쿼리 -->
	<insert id="creatCommunityInfoFile"
		parameterType="com.walab.hgu.DTO.FileDTO">
		INSERT INTO hguclub.communityInfoFile (communityInfoId,
		originalUrl)
		VALUES (#{communityInfoId}, #{originalUrl})
	</insert>
	
	<update id="updateViewCount" parameterType="int">
		update communityInfo set viewCount = viewCount + 1 where id = #{id}
	</update>

	<select id="countInfo" resultType="int">
		SELECT COUNT(*) as listCnt FROM communityInfo
	</select>
</mapper>