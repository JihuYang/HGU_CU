<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CommunityMaterial">

	<select id="readCommunityMaterial"
		resultType="com.walab.hgu.DTO.CommunityMaterialDTO"
		parameterType="hashmap">
		SELECT c.id, name as writer, title, c.regdate, viewCount FROM hguclub.communityMaterial as c
		INNER JOIN hguclub.user as u ON c.userId = u.id;
		
	</select>
	<select id="readCommunityMaterialDetail" 
		resultType="com.walab.hgu.DTO.CommunityMaterialDTO"
		parameterType="hashmap">
		SELECT cM.id, name as writer, title, content, cM.regdate,viewCount, originalUrl, realUrl, cMF.order
		FROM hguclub.communityMaterial as cM ,hguclub.user u,hguclub.communityMaterialFile as cMF
		WHERE cM.userId = u.id and cM.id = #{id} LIMIT 1
	</select>

</mapper>